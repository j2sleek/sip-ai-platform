# voice_core — example environment configuration (Phase 1)
# Copy to `.env` and adjust. NEVER commit `.env` or real secrets.

# Application environment: development | test | prod
APP_ENV=development

# Log level: debug | info | warning | error
LOG_LEVEL=info
