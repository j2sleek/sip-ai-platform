; SIP AI Platform — PJSIP Configuration (Development)
; Phase 2B: PJSIP + Linphone Telephony Foundation

[global]
type=global
endpoint_identifier_order=auth_username,username,ip,anonymous
default_realm=asterisk
user_agent=SIP-AI-Platform-Asterisk

; ============================================================================
; TRANSPORT
; ============================================================================

[transport-udp]
type=transport
protocol=udp
bind=0.0.0.0:5060

; ============================================================================
; LINPHONE CLIENT — 1001
; ============================================================================

[1001]
type=endpoint
context=internal

; Authentication
auth=1001-auth

; Registration AOR
aors=1001

; From header settings for registrar
from_user=1001
from_domain=10.171.202.39

; Codecs
disallow=all
allow=ulaw
allow=alaw

; NAT / Android / PRoot-friendly media settings
direct_media=no
force_rport=yes
rewrite_contact=yes
rtp_symmetric=yes

; DTMF
dtmf_mode=rfc4733

; Identify endpoint by username in From/To headers
identify_by=username

[1001-auth]
type=auth
auth_type=userpass
username=1001
password=1001_secure_password_here

[1001]
type=aor
max_contacts=1
remove_existing=yes

; ============================================================================
; AI SERVICE PLACEHOLDER — 2000
; ============================================================================

[2000]
type=endpoint
context=internal
auth=2000-auth
aors=2000-aor

disallow=all
allow=ulaw
allow=alaw

direct_media=no
force_rport=yes
rewrite_contact=yes
rtp_symmetric=yes

[2000-auth]
type=auth
auth_type=userpass
username=2000
password=2000_secure_password_here

[2000-aor]
type=aor
max_contacts=1
remove_existing=yes

; ============================================================================
; SYSTEM ENDPOINT
; ============================================================================

[system]
type=endpoint
context=internal
disallow=all
allow=ulaw
allow=alaw
