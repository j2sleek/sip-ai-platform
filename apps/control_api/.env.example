# control_api — example environment configuration (Phase 1)
# Copy to `.env` and adjust. NEVER commit `.env` or real secrets.

# Application environment: development | test | prod
APP_ENV=development

# HTTP bind
HOST=0.0.0.0
PORT=4000

# Log level: debug | info | warn | error
LOG_LEVEL=info
