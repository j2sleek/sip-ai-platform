# llm service — Phase 1 dependencies
#
# The Phase 1 foundation runs on the Python 3.14 standard library ONLY.
# Real LLM dependencies (e.g. an Ollama HTTP client) are added in the Python AI
# services phase — NOT during foundation work.
#
# Optional dev tooling (tests use stdlib unittest and don't require it):
#   pytest>=8,<10
